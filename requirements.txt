numpy==2.5.1
pandas==3.0.5
Pillow==12.3.0
requests==2.34.2
streamlit==1.60.0
streamlit-searchbox==0.1.24
python-dotenv==1.2.2
groq==1.6.0
geopy==2.5.0
redis==8.1.0
# Streamlit already depends on pydeck, but the day-trip map imports it directly, so it is
# pinned here rather than left to whatever Streamlit happens to resolve.
pydeck==0.9.3
